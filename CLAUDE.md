# 간단 가계부 — 프로젝트 규칙

<!-- 5회 실습의 정답 예시입니다. 이 파일이 있으면 Claude Code가 매 세션 자동으로 읽습니다. -->

이 프로젝트는 쓴 돈을 기록하고 합계·통계를 보여 주는 작은 웹 앱이다.
외부 라이브러리와 빌드 도구를 쓰지 않는다. 브라우저에서 파일을 직접 열어 실행한다.

## 반드시 지킬 것

1. **화면에 보이는 모든 금액은 `formatWon()`을 거친다.** 숫자를 그대로 찍지 않는다.
   금액 계산이 필요하면 `roundWon()`으로 원 단위를 먼저 맞춘다.
2. **주석은 한국어로 쓴다.** 변수·함수 이름은 영어, 설명은 한국어다.
3. **새로 만드는 통계 함수는 `src/stats.js`에 둔다.** 다른 파일에 통계 로직을 흩지 않는다.

## 코드 스타일

- ES 모듈(`import`/`export`)을 쓰지 않는다. 브라우저에서 파일을 직접 열면 막히기 때문이다.
- 각 파일 끝에 `if (typeof module !== 'undefined' && module.exports) { module.exports = { ... } }` 를 두어
  브라우저와 Node가 같은 파일을 쓰게 한다.
- `var`와 일반 `function` 선언을 쓴다. 이 저장소의 기존 코드와 모양을 맞춘다.

## 확인 방법

- 앱: `index.html`을 브라우저로 연다.
- 테스트: `tests.html`을 브라우저로 열거나 `node tests/run.js`를 돌린다.
- **코드를 고쳤으면 테스트를 돌려 결과를 알려 준다.**
